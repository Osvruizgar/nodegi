const stringToNumber = Number(process.argv[2]);
const stringToNumber2 = Number(process.argv[3]);
const stringToNumber3 = Number(process.argv[4]);
console.log(stringToNumber + stringToNumber2 + stringToNumber3);
